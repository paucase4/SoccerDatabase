# SoccerDatabase

Project consisting of building a database with Oracle Database. Given a document with specifications and various files, we created a schema for the database, created it and populated it. Then, made several queries to get insights on the data.(Academic Project in a team)

The project can be replicated with the data files and the code uploaded. It was done using Oracle Database.
